# dependencies
node_modules/
.pnpm-store/

# build output
dist/
.astro/
.output/
*.tsbuildinfo

# env (keep .env.example)
.env
.env.*
!.env.example

# logs
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

# test / coverage
test-results/
playwright-report/
playwright/.cache/
coverage/

# scripts (ignore local secrets/captures; keep committed .mjs tooling)
scripts/.*

# caches
.cache/
.vite/
.eslintcache

# deploy adapters
.vercel/
.netlify/

# editors
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea/
*.swp

# OS
.DS_Store
.DS_Store?
._*
Thumbs.db
ehthumbs.db
Desktop.ini

# design handoff / reference (local only)
.handoff/
/design/

# infobip pricing sync — local auth session, captured payloads, and the internal
# rate reference (confidential negotiated rates). Never commit.
scripts/.infobip-session.json
scripts/.captures/
scripts/.infobip-rates.internal.json

# Session scratch artifacts (template import forensics etc.)
tmp/
tests/e2e/.auth/

# Generated dashboard presentation assets (mp3/mp4/webm)
presentation-out/
