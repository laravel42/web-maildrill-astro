// Re-export for back-compat. `Wrapper` moved to `@eb/email-builder`
// (Node-safe, shared by block renderers). See L42 Node HTML renderer work.
export { Wrapper as default } from '@eb/email-builder';
