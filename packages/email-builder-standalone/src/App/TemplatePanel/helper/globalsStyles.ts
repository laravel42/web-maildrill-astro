// Re-export for back-compat. `globalsStyles` moved to `@eb/email-builder`
// (Node-safe, shared by the HTML export and the Node renderer).
// See L42 Node HTML renderer work.
export { globalsStyles } from '@eb/email-builder';
