/** Side panel widths — aligned with email-builder-standalone chrome. */
export const LIBRARY_FULL_WIDTH = 256;
export const LIBRARY_COMPACT_WIDTH = 164;
// Wide enough for the body formatting bar (8 buttons + 4 dividers ≈ 301px)
// to lay out without scrolling inside the panel's 16px padding.
export const INSPECTOR_FULL_WIDTH = 335;
export const INSPECTOR_COMPACT_WIDTH = 56;

export const PANEL_TRANSITION_MS = 220;
export const PANEL_TRANSITION = `width ${PANEL_TRANSITION_MS}ms cubic-bezier(0.4, 0, 0.2, 1)`;
